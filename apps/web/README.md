# QuitLoop website

Public marketing site and web companion for the QuitLoop mobile app.

```bash
cd apps/web
npm install
npm run dev
```

Open http://localhost:3000.

The portal is a front-end demo. It stores a session flag in `localStorage` and does not call the .NET API yet.
