# QuitLoop mobile

React Native + TypeScript client (Expo). This is a runnable shell; product screens come later.

```bash
npm install
npx expo start
```

API base URL defaults to https://api.quitloop.org. Override with `EXPO_PUBLIC_API_URL` (see `.env.example`).
